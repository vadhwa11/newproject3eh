/*window.onload = function() {
       var formlist = document.getElementsByTagName("form");
       if(formlist.length > 0)
       {
            for(var i=0; i<formlist.length; i++)
           {
            	
            	if(formlist[i].getAttribute("name")!='observation_ward_Waiting_List' &&  formlist[i].getAttribute("name")!='snomedTemplates'
            	&& formlist[i].getAttribute("name")!='opd_OP_Clinic_Waiting_PatientList' &&  formlist[i].getAttribute("name")!='mlcSearch'
            	&& formlist[i].getAttribute("name")!='opd_nurse_waiting_PatientList'){
            	
             	if(formlist[i].elements[csrfTokenName]){	
            		var toRemoveOldToken = formlist[i].elements[csrfTokenName];
            	   		if(toRemoveOldToken.parentNode)
            	   			toRemoveOldToken.parentNode.removeChild(toRemoveOldToken);
               	}
               
               var ele = document.createElement("input");
               ele.setAttribute("name",csrfTokenName);
               ele.setAttribute("type","hidden");
               ele.setAttribute("value",csrfTokenValue);
               
               formlist[i].appendChild(ele); 
           } 
            	
           }
    }
}*/